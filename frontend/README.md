# Privacy-Aware RAG — Frontend

React 18 + Vite + Tailwind CSS (v3.3.3) frontend for Privacy-Aware RAG. Built to run in Docker Compose and connect to backend at `http://api:3001`.

## Quick setup (local)

1. From project root:
   ```powershell
   cd C:\Privacy-Aware-RAG\frontend
   npm install
   npm start
